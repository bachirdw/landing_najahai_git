// Site-wide links, centralized so every "Get started" / "Add to Chrome" button
// updates from one place instead of being edited page by page.

export const APP_URL = "https://app.najahai.com.au";

// TODO: replace PLACEHOLDER once the extension is published — the real URL is
// shown on the Chrome Web Store developer dashboard after review. Format is
// https://chromewebstore.google.com/detail/<name-slug>/<32-char-extension-id>
// (the id is assigned by Google at first upload, not chosen).
export const CHROME_STORE_URL =
  "https://chromewebstore.google.com/detail/najah-ai/PLACEHOLDER_EXTENSION_ID";
